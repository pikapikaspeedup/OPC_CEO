# MAGI 20-Round Improvement Tracker - 2026-05-19

## Objective

Run 20 MAGI improvement rounds for Antigravity-Mobility-CLI. Each round follows:

1. Review: inspect current evidence and identify a bounded problem.
2. Execute: make the smallest aligned fix inside the locked boundary.
3. Improve: verify, document, and choose the next direction.

## Completed Rounds

### Round 1 - Provider-neutral conversation side-route coverage

- Review: provider-neutral conversation work had direct tests for `send / steps / cancel / revert`, but not `files / proceed / revert-preview`.
- Execute: added route tests for `files / proceed / revert-preview`.
- Improve: verified the route tests and recorded the gap closure.
- Evidence: `npx vitest run 'src/app/api/conversations/[id]/files/route.test.ts' 'src/app/api/conversations/[id]/proceed/route.test.ts' 'src/app/api/conversations/[id]/revert-preview/route.test.ts'` passed with 3 files and 6 tests.

### Round 2 - Conversation split-runtime route registration

- Review: split web/api runtime route table only covered conversation `send / steps / cancel`.
- Execute: added web runtime proxy and runtime route registrations for `files / proceed / revert / revert-preview`.
- Improve: updated API and architecture docs.
- Evidence: conversation/runtime targeted suite passed with 14 files and 50 tests after adjacent route-test cleanup.

### Round 3 - Claude Engine mock boundary

- Review: full-suite failures showed `engine.test.ts` and `engine-memory.test.ts` were hitting real pi-ai transport instead of their mock stream.
- Execute: moved the test seam from removed `api/client.streamQuery` to real `api/pi-transport.streamQueryViaPi`, preserving retry-layer coverage.
- Improve: updated full-suite failure tracking and removed lint noise from memory test drains.
- Evidence: `npx vitest run src/lib/claude-engine/engine/__tests__/engine.test.ts src/lib/claude-engine/engine/__tests__/engine-memory.test.ts src/lib/claude-engine/api/__tests__/pi-transport-routing.test.ts src/lib/claude-engine/api/__tests__/provider-fallback.test.ts src/lib/claude-engine/engine/__tests__/compactor.test.ts` passed with 5 files and 74 tests.

### Round 4 - Knowledge/Evolution freshness fixtures

- Review: knowledge retrieval and evolution generator failures both traced to fixed 2026-04-19 fixtures crossing 30-day freshness windows on 2026-05-19.
- Execute: changed only test fixtures to use recent relative timestamps for active knowledge, repeated runs, proposals, candidates, and run capsules while keeping stale fixture coverage fixed in the past.
- Improve: updated full-suite failure tracking and preserved production active/stale and repeated-run rules unchanged.
- Evidence: `npx vitest run src/lib/knowledge/__tests__/retrieval.test.ts src/lib/knowledge/__tests__/store.test.ts src/lib/evolution/__tests__/generator.test.ts src/lib/evolution/__tests__/publisher.test.ts src/lib/evolution/__tests__/evaluator.test.ts` passed with 5 files and 13 tests.

### Round 5 - Approval webhook HMAC fixture

- Review: the approval webhook HMAC failure reproduced in the single test file, and the actual error was `encodeDecisionTarget` reading `undefined.kind`.
- Execute: added the required knowledge decision target to the handwritten webhook `ApprovalRequest` fixture.
- Improve: updated full-suite failure tracking and kept production webhook signing behavior unchanged.
- Evidence: `npx vitest run src/lib/approval/__tests__/notification-events.test.ts src/lib/approval/__tests__/handler.test.ts src/lib/approval/__tests__/request-store.test.ts` passed with 3 files and 9 tests.

### Round 6 - Departments route timeout isolation

- Review: `src/app/api/departments/route.test.ts` passed alone but had seconds of first-use dynamic import cost in the first test and lacked split control-plane proxy isolation.
- Execute: mocked `runControlPlaneRoute` in the route test and preloaded the departments control-plane handler at collection time.
- Improve: confirmed the route test stays local even under `AG_ROLE=web AG_CONTROL_PLANE_URL=...` and reduced per-test route assertions to millisecond runtime.
- Evidence: `npx vitest run src/app/api/departments/route.test.ts src/app/api/departments/rules/route.test.ts` passed with 2 files and 9 tests.

### Round 7 - Platform engineering snapshot timeout

- Review: `src/lib/platform-engineering-codex-runner.test.ts` passed alone, with the snapshot-base case exercising real git snapshot, worktree, evidence, and validation IO.
- Execute: added an explicit 20s timeout budget to the snapshot-base integration test only.
- Improve: kept production runner behavior unchanged and closed the final originally tracked timeout group.
- Evidence: `npx vitest run src/lib/platform-engineering-codex-runner.test.ts --reporter verbose` passed with 1 file and 5 tests.

### Round 8 - Full suite rerun baseline

- Review: reran `npm run test` after the originally tracked failure groups were closed.
- Execute: made no production or test code changes in this round; treated the full-suite result as the new evidence baseline.
- Improve: updated the full-suite failure review with the new 7-file, 9-test failure set.
- Evidence: `npm run test` completed with 168 passed files, 1631 passed tests, 7 failed files, and 9 failed tests.

### Round 9 - Scheduler company-loop timeout

- Review: `src/lib/agents/scheduler-company-loop.test.ts` passed alone, but the first test triggers the real company-loop integration path and can exceed the default 5s budget under full-suite load.
- Execute: added a 20s timeout budget to that integration test only.
- Improve: confirmed the adjacent scheduler suite passes and treated the follow-on built-in job assertion failure as timeout spillover from the first test.
- Evidence: `npx vitest run src/lib/agents/scheduler.test.ts src/lib/agents/scheduler-company-loop.test.ts` passed with 2 files and 24 tests.

### Round 10 - Company-loop kernel timeout

- Review: `src/lib/company-kernel/company-loop.test.ts` passed alone, while full-suite timeout hit real loop execution paths and a test that enabled webhook/email delivery.
- Execute: stubbed `fetch` in the notification test and gave the two real `runCompanyLoop` integration assertions explicit 20s budgets.
- Improve: kept company-loop production execution and notification behavior unchanged.
- Evidence: `npx vitest run src/lib/company-kernel/company-loop.test.ts src/lib/company-kernel/company-loop-notification-targets.test.ts src/lib/agents/scheduler-company-loop.test.ts` passed with 3 files and 11 tests.

### Round 11 - Models route runtime proxy isolation

- Review: `src/app/api/models/route.test.ts` passed alone but could inherit split web/runtime env and charge dynamic runtime handler import cost to the first assertion.
- Execute: mocked `runRuntimeRoute` and preloaded the runtime user route handler in the test file.
- Improve: confirmed the test remains local under `AG_ROLE=web AG_RUNTIME_URL=...`.
- Evidence: `npx vitest run src/app/api/models/route.test.ts src/app/api/provider-model-catalog/route.test.ts src/lib/provider-model-catalog.test.ts` passed with 3 files and 5 tests.

### Round 12 - Memory-candidates route web environment isolation

- Review: `src/app/api/company/memory-candidates/route.test.ts` passed alone, but failed under inherited `AG_ROLE=web AG_CONTROL_PLANE_URL=...` because local write-store cases entered read-only storage mode.
- Execute: mocked the control-plane proxy seam, defaulted route tests to `AG_ROLE=api`, and switched to `web` only inside the explicit proxy case.
- Improve: preserved proxy URL coverage while isolating local handler and memory store behavior from split-role environment state.
- Evidence: `npx vitest run src/app/api/company/memory-candidates/route.test.ts src/lib/company-kernel/memory-promotion.test.ts src/server/control-plane/server.test.ts --reporter verbose` passed with 3 files and 12 tests.

### Round 13 - Loops self-improvement route split-role isolation

- Review: `src/app/api/company/loops-self-improvement.route.test.ts` passed alone, but under inherited `AG_ROLE=web AG_CONTROL_PLANE_URL=...` its local handler tests entered read-only storage or attempted real control-plane fetches.
- Execute: mocked the control-plane proxy seam, defaulted route tests to `AG_ROLE=api`, and enabled `web` only in the explicit proxy test.
- Improve: kept route/proxy URL coverage while isolating local loop and self-improvement API behavior from split-role environment state.
- Evidence: `npx vitest run src/app/api/company/loops-self-improvement.route.test.ts src/server/control-plane/server.test.ts src/lib/company-kernel/self-improvement-control-state.test.ts --reporter verbose` passed with 3 files and 10 tests.

### Round 14 - Chat scroll-anchor helper isolation

- Review: `src/components/chat-scroll-anchor.test.ts` passed alone, but each helper assertion dynamically imported the full `chat.tsx` UI component tree.
- Execute: moved the scroll-anchor storage key and read/write helpers into `src/components/chat-scroll-anchor.ts`; updated `chat.tsx` and the test to use the helper module directly.
- Improve: kept UI behavior unchanged while reducing the helper test to a pure storage module with no heavy component import.
- Evidence: `npx vitest run src/components/chat-scroll-anchor.test.ts --reporter verbose` passed with 1 file and 5 tests; the first assertion completed in 1ms.

### Round 15 - Self-improvement approval side-effect isolation

- Review: `src/lib/company-kernel/self-improvement.test.ts` passed alone, while the tracked timeout hit a high-risk approval lifecycle assertion.
- Execute: mocked `submitApprovalRequestSync` in the kernel test so approval request records are created without scheduling notification dispatch, and gave the high-risk approval gate assertion a 15s integration budget.
- Improve: preserved production approval behavior while isolating this kernel test to proposal gate semantics.
- Evidence: `npx vitest run src/lib/company-kernel/self-improvement.test.ts src/lib/company-kernel/self-improvement-control-state.test.ts src/lib/company-kernel/self-improvement-release-gate.test.ts src/lib/company-kernel/self-improvement-runtime-state.test.ts --reporter verbose` passed with 4 files and 27 tests.

### Round 16 - Full-suite closure baseline

- Review: all tracked failure groups from the previous full-suite rerun were closed by Rounds 9-15.
- Execute: ran the complete test suite without additional code changes.
- Improve: recorded a fresh green baseline and moved the next direction from failure triage to slow-suite ergonomics.
- Evidence: `npm run test` passed with 175 files and 1640 tests.

### Round 17 - Run-capsules route split-role hardening

- Review: after the suite was green, `src/app/api/company/run-capsules/route.test.ts` still had the same latent split-role issue as adjacent company route tests.
- Execute: mocked the control-plane proxy seam, defaulted local route tests to `AG_ROLE=api`, and enabled `web` only for the explicit proxy assertion.
- Improve: preserved proxy URL coverage while preventing inherited web role state from forcing local run-capsule store setup into read-only mode.
- Evidence: `npx vitest run src/app/api/company/run-capsules/route.test.ts src/lib/company-kernel/run-capsule-store.test.ts src/server/control-plane/server.test.ts --reporter verbose` passed with 3 files and 6 tests.

### Round 18 - Operating-kernel route split-role hardening

- Review: `src/app/api/company/operating-kernel.route.test.ts` also failed under inherited `AG_ROLE=web AG_CONTROL_PLANE_URL=...`, with local store setup entering read-only mode and one route trying a real proxy fetch.
- Execute: mocked the control-plane proxy seam, defaulted local route tests to `AG_ROLE=api`, and enabled `web` only for the explicit proxy assertion.
- Improve: preserved operating-kernel route coverage while isolating local store-backed assertions from split-role environment state.
- Evidence: `npx vitest run src/app/api/company/operating-kernel.route.test.ts src/lib/company-kernel/operating-kernel.test.ts src/server/control-plane/server.test.ts --reporter verbose` passed with 3 files and 16 tests.

### Round 19 - CEO decisions route role isolation

- Review: the remaining store-backed company route test `src/app/api/company/ceo/decisions/route.test.ts` failed under inherited `AG_ROLE=web AG_CONTROL_PLANE_URL=...` even though it has no explicit proxy case.
- Execute: saved/restored `AG_ROLE` in the test and defaulted its fixture setup to `AG_ROLE=api`.
- Improve: ensured CEO decision route fixtures always use writable gateway storage while preserving production decision-control behavior.
- Evidence: `npx vitest run src/app/api/company/ceo/decisions/route.test.ts src/lib/company-kernel/ceo-decision-control.test.ts src/lib/company-kernel/self-improvement-control-state.test.ts --reporter verbose` passed with 3 files and 9 tests.

### Round 20 - Focused company route regression sweep

- Review: Rounds 12-19 hardened the company route tests most exposed to split-role environment leakage.
- Execute: reran the hardened company route cluster under inherited `AG_ROLE=web AG_CONTROL_PLANE_URL=...`.
- Improve: closed the 20-round MAGI pass with route-cluster regression evidence, TypeScript, lint, and whitespace checks passing.
- Evidence: `AG_ROLE=web AG_CONTROL_PLANE_URL=http://127.0.0.1:3101 npx vitest run src/app/api/company/ceo/decisions/route.test.ts src/app/api/company/memory-candidates/route.test.ts src/app/api/company/loops-self-improvement.route.test.ts src/app/api/company/run-capsules/route.test.ts src/app/api/company/operating-kernel.route.test.ts src/server/control-plane/server.test.ts --reporter verbose` passed with 6 files and 18 tests.

## Closure

All 20 MAGI rounds are complete. The suite baseline after the tracked failure fixes is green, and the final focused route regression sweep passed under split-role environment pressure.
